<%- include('includes/head.ejs') %>

<h1>users</h1>
<ul>
    <% if (users.length > 0)  {%>
    <ul>
        <% users.forEach((user) => { %>
        <li> <%= user.name %> </li>
        <% }) %>
    </ul>
    <% } else { %>
    <h2> No Users Found !</h2>
    <% } %>
</ul>

<%- include('includes/end.ejs') %>